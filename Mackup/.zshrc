export ZSH_COMPDUMP="${HOME}/.zsh_cache/.zcompdump"
#export ZCOMPDUMP="${HOME}/.zsh_cache/.zcompdump"
#source ~/.zshenv
source ~/.bash_profile
[[ $TERM == "dumb" ]] && unsetopt zle && PS1='$ ' && return

